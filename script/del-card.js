/*const cardContainer = document.querySelector('.elements');
const cardItem = cardContainer.querySelector('.elements__card');
const delButton = cardContainer.querySelector('.elements__delete');

/*function delCard () {
    cardContainer.removeChild(cardItem);
    cardContainer.forEach(cardContainer);
    console.log(event.target);
};

cardItem.addEventListener('click', function(evt) {
    //evt.target = delButton;
    evt.target.classList.add('elements__card_hide');
});

/*delButton.addEventListener('click', function(evt) {
        evt.preventDefault();
        evt.delButton.cardContainer.removeChild(cardItem);
});*/


/*delButton.addEventListener('click', function () {
    const delCard = delButton.closest('.elements__card');
   delCard.remove();
  });

cardContainer.querySelector('.elements__like').addEventListener('click', function() {
    const cards = document.querySelectorAll('.elements__like');
    cards.forEach ((item) => {
    item.classList.toggle('elements__like_liked');
    console.log(item);
    });
});*/

/*cardContainer.querySelector('.elements__delete').addEventListener('click', function() {
    const delCard = delButton.closest('.elements__card');
    delCard.remove();
});*/
/*
delButton.addEventListener('click', function () {
    const cards = document.querySelectorAll('.elements__delete');
    cards.forEach ((item) => {
        const toDel = item.closest('.elements__card')
      toDel.remove();
    });
    });
    */